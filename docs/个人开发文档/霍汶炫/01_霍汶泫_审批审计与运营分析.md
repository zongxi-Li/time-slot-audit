# 霍汶泫（2413041630）开发任务书 / Codex 提示词

## 角色定位

你负责 **administration：特殊会议室审批、管理员运营、系统审计与统计分析域** 的全栈开发。

现有系统已经具备：登录/JWT、会议室读取、预约创建/查询/取消、`PENDING/CONFIRMED/REJECTED/CANCELLED` 状态模型、`approval_record` 与 `operation_log` 基础表。你的任务是在不越过 reservation 数据所有权的前提下，将管理员侧做成可独立演示的完整闭环。

# 公共执行规范（所有成员必须遵守）

仓库：`https://github.com/zongxi-Li/time-slot-audit.git`

当前团队只保留 `main` 作为共享基线。任何成员**禁止直接在 main 上开发或 push**。开始任务前必须执行：

```bash
git switch main
git pull --ff-only origin main
git status
```

确认工作区干净后，再创建自己的功能分支。所有代码、SQL、测试和文档只提交到自己的 feature 分支，最终通过 Pull Request 合入 `main`，禁止自行 merge `main`。

## Git 提交规范

统一使用 Conventional Commits：

```text
<type>(<scope>): <简短说明>
```

允许的 `type`：`feat`、`fix`、`refactor`、`test`、`docs`、`chore`、`ci`。

`scope` 必须使用当前负责 Domain，例如：`administration`、`identity`、`meeting`、`resource`、`reservation`。公共工程任务可使用 `ci`、`build`、`docs`。

禁止以下提交信息：`update`、`修改`、`完成任务`、`最终版`、`111`。

推荐：

```text
feat(resource): 实现会议室与分类管理
fix(reservation): 限制取消操作仅允许预约本人
refactor(meeting): 收敛签到状态转换逻辑
test(identity): 补充禁用账号与越权场景测试
docs(administration): 补充审批模块接口说明
```

每个 commit 应保持**单一职责、可独立解释、可回滚**。不要把数据库、后端、前端、测试全部堆成一个超大 commit。

## 并行开发约束

1. 不直接访问或修改其他 Domain 的 Mapper / 数据表；跨域写操作必须调用对方公开 Service。
2. 不修改历史 migration；每个成员只新增自己被分配的 migration 文件。
3. 为降低五人并行冲突，本轮 feature 分支**不要直接重写 `sql/schema.sql`**；只新增 migration。最终由组长在集成阶段统一同步完整快照。
4. 不擅自修改冻结的 `core-business-contract.md`。如发现必须改变核心语义，新增 `docs/development/proposals/<domain>-contract-change.md` 并停止相关实现，交由组长评审。
5. 新前端业务代码优先放入 `frontend/src/modules/<domain>/`；避免继续把新业务堆进全局 `stores/`、`views/`、`shared/api/index.ts`。
6. 对 `router/index.ts`、`MainLayout.vue`、全局菜单等共享文件的改动必须小而独立，单独 commit，方便组长处理冲突。
7. 不改别人负责的 UI 页面，不顺手实现其他 Domain 的需求。
8. 不引入 Redis、MQ、微服务、工作流引擎或复杂 RBAC，除非本任务明确要求。
9. API 统一返回 `ApiResponse<T>`；错误码遵守现有全局约定。
10. 预约时间语义继续遵守 `[start,end)`，`PENDING/CONFIRMED` 占用时间，`REJECTED/CANCELLED` 不占用。

## 合并前统一验证

如同时改前后端，则全部执行：

```bash
cd frontend
npm ci
npm run type-check
npm run build

cd ../backend
mvn clean test
```

涉及数据库 migration 的任务必须在 PR 说明中写清：新增 migration 文件、升级前置版本、验证方式。

最终推送：

```bash
git status
git log --oneline --decorate -n 10
git push -u origin <你的分支名>
```

如果开发期间 `main` 有新提交，在最终测试前：

```bash
git fetch origin
git rebase origin/main
```

若你的 feature 分支已经推送过，rebase 后只允许：

```bash
git push --force-with-lease
```

禁止 `git push --force`。

最终不要自行合并 PR。由组长做集成审查。

## 你的分支

```bash
git switch -c feature/administration-core
```

## 数据所有权

你拥有：`approval_record`、`operation_log`、运营统计读取模型；如确有需要，可新增 `announcement`（公告）表。

你的 migration 固定使用：

```text
sql/migrations/V1_6__administration_management.sql
```

禁止直接调用 `ReservationMapper.updateStatus()`、直接 UPDATE `reservation`、修改 identity/resource/meeting 的表结构。

审批必须调用 reservation 域公开生命周期 Service。若当前 `ReservationLifecycleService` 尚未在 `main` 提供，不要偷写 Mapper；先实现 administration 自己可独立完成的部分，并在 `docs/development/proposals/administration-reservation-lifecycle.md` 记录所需接口：`approve(reservationId, operator)`、`reject(reservationId, operator, reason)`、`forceCancel(...)` 等，等待组长合并契约。

## P0：必须完成

### 1. 待审批预约与审批闭环

实现：

```text
GET  /api/admin/reservations?status=PENDING
GET  /api/admin/reservations/{id}
POST /api/admin/reservations/{id}/approve
POST /api/admin/reservations/{id}/reject
```

要求：仅 `ADMIN` 可访问；仅 `PENDING` 可审批；APPROVE 为 `PENDING -> CONFIRMED`；REJECT 为 `PENDING -> REJECTED`；驳回原因必填；重复审批返回明确业务错误；每次审批写 `approval_record` 与 `operation_log`；禁止用 `CANCELLED` 表示驳回。

### 2. 管理员操作审计

至少覆盖 `APPROVE_RESERVATION`、`REJECT_RESERVATION`、`FORCE_CANCEL_RESERVATION`（若生命周期接口已提供）。提供：

```text
GET /api/admin/audit-logs
```

至少支持按操作人、业务类型、时间范围查询。

### 3. 管理后台前端

新代码放：`frontend/src/modules/administration/`。

至少实现：待审批列表、审批详情、通过/驳回、审批历史、审计日志列表。真实模式必须使用后端数据，禁止写死统计数据伪装完成。

## P1：建议完成

运营统计：会议室利用率、热门会议室 Top N、高峰时段、取消率、No-Show 率（meeting 未接入时不得伪造）。至少实现 2 类真实图表。

数据导出：至少完成一种真实 CSV/Excel 导出，可选审批记录、审计日志或利用率统计。

公告管理放到最后，不得挤占审批/审计核心闭环。

## 测试要求

覆盖：USER 访问 admin API -> 403；审批通过；审批驳回；重复审批；approval_record 与 operation_log 写入；审计查询过滤；统计结果来自真实数据。

## 推荐提交拆分

```text
feat(administration): 实现待审批查询与审批流程
feat(administration): 增加审批历史与操作审计
feat(administration): 接入管理员审批页面
feat(administration): 增加运营统计与数据导出
test(administration): 覆盖审批权限与状态流转
docs(administration): 补充管理域接口与验收说明
```

## 最终交付

新增 `docs/development/deliveries/administration-final-report.md`，写明接口、migration、前端页面、测试结果、未完成 P1、跨域依赖、Git commit 列表。

最终验收：**特殊会议室必须能够真实完成“待审批 -> 通过/驳回”，审批记录与关键管理员操作可追溯，统计看板不能使用前端写死数据。**
