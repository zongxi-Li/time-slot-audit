# 张陈浩（2413042116）开发任务书 / Codex 提示词

## 角色定位

你负责 **identity：用户身份、组织、账号状态、预约资格、信用/违规与黑名单规则域** 的全栈开发。

当前系统已有 `sys_user`、登录、BCrypt、JWT、`USER/ADMIN` 两级角色和 `/api/users/me`。你的任务是把身份域扩展成真正可管理、可限制预约资格的业务模块，但不要建设复杂 RBAC。

# 公共执行规范（所有成员必须遵守）

仓库：`https://github.com/zongxi-Li/time-slot-audit.git`

当前团队只保留 `main` 作为共享基线。任何成员**禁止直接在 main 上开发或 push**。开始任务前必须执行：

```bash
git switch main
git pull --ff-only origin main
git status
```

确认工作区干净后，再创建自己的功能分支。所有代码、SQL、测试和文档只提交到自己的 feature 分支，最终通过 Pull Request 合入 `main`，禁止自行 merge `main`。

## Git 提交规范

统一使用 Conventional Commits：

```text
<type>(<scope>): <简短说明>
```

允许的 `type`：`feat`、`fix`、`refactor`、`test`、`docs`、`chore`、`ci`。

`scope` 必须使用当前负责 Domain，例如：`administration`、`identity`、`meeting`、`resource`、`reservation`。公共工程任务可使用 `ci`、`build`、`docs`。

禁止以下提交信息：`update`、`修改`、`完成任务`、`最终版`、`111`。

推荐：

```text
feat(resource): 实现会议室与分类管理
fix(reservation): 限制取消操作仅允许预约本人
refactor(meeting): 收敛签到状态转换逻辑
test(identity): 补充禁用账号与越权场景测试
docs(administration): 补充审批模块接口说明
```

每个 commit 应保持**单一职责、可独立解释、可回滚**。不要把数据库、后端、前端、测试全部堆成一个超大 commit。

## 并行开发约束

1. 不直接访问或修改其他 Domain 的 Mapper / 数据表；跨域写操作必须调用对方公开 Service。
2. 不修改历史 migration；每个成员只新增自己被分配的 migration 文件。
3. 为降低五人并行冲突，本轮 feature 分支**不要直接重写 `sql/schema.sql`**；只新增 migration。最终由组长在集成阶段统一同步完整快照。
4. 不擅自修改冻结的 `core-business-contract.md`。如发现必须改变核心语义，新增 `docs/development/proposals/<domain>-contract-change.md` 并停止相关实现，交由组长评审。
5. 新前端业务代码优先放入 `frontend/src/modules/<domain>/`；避免继续把新业务堆进全局 `stores/`、`views/`、`shared/api/index.ts`。
6. 对 `router/index.ts`、`MainLayout.vue`、全局菜单等共享文件的改动必须小而独立，单独 commit，方便组长处理冲突。
7. 不改别人负责的 UI 页面，不顺手实现其他 Domain 的需求。
8. 不引入 Redis、MQ、微服务、工作流引擎或复杂 RBAC，除非本任务明确要求。
9. API 统一返回 `ApiResponse<T>`；错误码遵守现有全局约定。
10. 预约时间语义继续遵守 `[start,end)`，`PENDING/CONFIRMED` 占用时间，`REJECTED/CANCELLED` 不占用。

## 合并前统一验证

如同时改前后端，则全部执行：

```bash
cd frontend
npm ci
npm run type-check
npm run build

cd ../backend
mvn clean test
```

涉及数据库 migration 的任务必须在 PR 说明中写清：新增 migration 文件、升级前置版本、验证方式。

最终推送：

```bash
git status
git log --oneline --decorate -n 10
git push -u origin <你的分支名>
```

如果开发期间 `main` 有新提交，在最终测试前：

```bash
git fetch origin
git rebase origin/main
```

若你的 feature 分支已经推送过，rebase 后只允许：

```bash
git push --force-with-lease
```

禁止 `git push --force`。

最终不要自行合并 PR。由组长做集成审查。

## 你的分支

```bash
git switch -c feature/identity-governance
```

## 数据所有权

你拥有 `sys_user`、`department`（新增）、用户资格/信用/违规相关表。

migration 固定：

```text
sql/migrations/V1_3__identity_governance.sql
```

推荐最小模型：`department`、`sys_user.department_id`、`user_violation`；预约资格可由账号状态、信用分、限制截止时间推导。不要创建 `role_permission/user_role/permission` 等复杂 RBAC 表。

## P0：必须完成

### 1. 安全加固

当前真实 API 模式不得存在公开默认 JWT 密钥。要求 `timeslot.jwt.secret` 必须显式来自环境变量 `JWT_SECRET`；未提供时真实后端 fail-fast。测试环境使用独立 test property。

### 2. 用户/部门管理

实现管理员接口：用户列表/详情/新增/修改/启停，部门列表/新增/修改。要求 USER 不能访问管理接口；username 唯一；密码仅保存 BCrypt；禁用账号不能登录；`/api/users/me` 返回真实个人资料与部门信息。

### 3. 预约资格判定 Service

向 reservation 域暴露只读公开能力，例如 `BookingQualificationService.check(userId)`，返回 `eligible/reason/creditScore/restrictedUntil`。reservation 不得直接访问 identity Mapper。

至少实现：账号禁用、黑名单/限制期、信用不足、正常用户四类判定。

### 4. 前端

新代码放：`frontend/src/modules/identity/`。

实现：个人资料、管理员用户列表/详情、账号启停、部门管理、违规/信用记录展示、预约资格状态展示。

## P1：建议完成

预约次数限制、违规自动扣分、黑名单自动进入/解除。管理员人工调整信用必须记录原因。

## 测试要求

覆盖：正确/错误密码；禁用账号；USER 管理接口 403；ADMIN 管理；BCrypt；正常/限制/黑名单资格；无 JWT_SECRET 的真实配置失败，测试配置正常。

## 推荐提交拆分

```text
fix(identity): 移除公开 JWT 默认密钥
feat(identity): 增加部门与用户资料管理
feat(identity): 实现账号状态与预约资格判定
feat(identity): 增加信用违规与黑名单规则
feat(identity): 接入用户与资格管理页面
test(identity): 覆盖认证权限与预约资格场景
docs(identity): 补充身份域接口与规则说明
```

## 最终交付

新增 `docs/development/deliveries/identity-final-report.md`。

最终验收：**真实登录与服务端权限可用；账号禁用、预约资格、信用/违规/黑名单规则真实影响业务判定，不能只做前端标签。**
